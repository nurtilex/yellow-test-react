Добрый день. 
По ходу выполнения тестового задания столкнулся с несколькими проблемами: 
1. Получение токена. 
По ТЗ по нажатии на let me in получить токен и сохранить его в локальном хранилище, после чего отобразить список всех пробежек текущего пользователя.

Насколько я понял при нажатии на кнопку я должен сделать запрос на бекенд и оттуда получить токен. Затем сохранить его в localStorage и использовать в дальнейшем при запросах.

Проблема в том, что в Swagger нет подходящего запроса для получения токена. 
Есть POST /v1/auth/uuidLogin, но он требует токен. Я так понял это запрос для авторизации, а не для регистрации.
Есть POST /v1/auth/register_push, но он тоже требует токен и я не уверен, что он предназначен для регистрации
В разделе auth есть еще один более менее подходящий метод, а именно GET /v1/auth/user. 
Если при запросе передать токен из ТЗ, то сервер возвращает объект с информацией о некой Каролине и id 3.  

Исходя из этой проблемы у меня пока не получилось в полной мере реализовать авторизацию по ТЗ, но я сделал кое-что другое.

Я хотел уточнить эти моменты в субботу, но насколько я понял вы были на выходных, поэтому я решил попробовать небольшой костыль, чтобы не сидеть, сложа руки.
Что я сделал?

При клике на LET ME IN я делаю запрос GET /v1/auth/user с токеном из ТЗ и получаю информацию о текущем пользователе, а обычно это Каролина с id 3. Дальше я нашел только один метод для получения списка пробежек, а именно GET /v1/data/sync, который возвращает список всех пробежек и юзеров. Из этого списка я вытягиваю пробежки Каролины и отображаю на странице. 

Я знаю, что это не то,что было прописано в ТЗ, но из-за возникших проблем и приближающемуся дедлайну я решил попробовать сделать по-другому. 

2. Тесты
Я не совсем уверен что правильно понял фразу Тесты для Swagger UI. 
Насколько я понял я должен написать тесты для моего взаимодействия с Api, но делать это я пока не стал из-за непонятки со Swagger-ом. Также скорее всего подразумевается написание тестов для компонентов, но и в необходимости этого я пока тоже не уверен. 

На этом вопросов нет. Версия моего проекта готова, но я пока не заливаю на Heroku. 
В целом я решил пожертвовать скоростью и оптимизацией приложения и использовал Redux Store и Redux-Saga, так как думаю в этом задании важнее показать что ты владеешь стеком прописанным в вакансии.

Жду вашего ответа и готов внести правки в проект, если потребуется.

